#include(iostream)
#include(fstream)
Using namespace std;

Int  rraySum
Int(n
{
Int *a,int sum=0
a=new int[n
if stream in(zuoye.txt)
if(!in)
cout<<“件无法打开”；
for(int i=0,int>>a[i],i<ni++)
sum+=a[i];
cout<<sum<<endl;
in.close();
}
Int main (void)
Int a;
Cout<<”输入数据长度：”;
Cin>>a;
Cout<<”求数据的和是：”;
Arraysum(a);
Return 0;
}
